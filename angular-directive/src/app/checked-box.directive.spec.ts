import { CheckedBoxDirective } from './checked-box.directive';

describe('CheckedBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckedBoxDirective();
    expect(directive).toBeTruthy();
  });
});
